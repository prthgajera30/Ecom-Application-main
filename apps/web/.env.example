NEXT_PUBLIC_API_BASE=http://localhost:4000/api
# Optional: override the websocket endpoint used by the frontend. If omitted the
# client will connect to the same origin the app was served from. Example:
# NEXT_PUBLIC_WS_URL=http://localhost:4000
NEXT_PUBLIC_WS_URL=http://localhost:4000
